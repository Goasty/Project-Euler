#https://projecteuler.net/problem=35
import sympy
from itertools import permutations
y = []

z = [x for x in sympy.primerange(1,1000000)]
b = [''.join(p) for p in permutations(str(x))]
for x in z:
    for a sympy.isprime()
    else:
        continue
